# Секреты — НИКОГДА не коммитить
.env
.env.*
*creds*.json
google_creds.json
**/google_creds.json
*.pem
*.key
service_account*.json

# Локальные туннели / автозапуск
.webapp_url
cloudflared.exe
ngrok.exe
*.cf_*
*.lhr_*
# Туннели/диагностика — локальные текстовые логи (.ngrok_*, .pinggy_*, .serveo_*, .remote_*, .tunnel_*)
.*_out.txt
.*_err.txt
.ssh_test.txt
.lhr*.txt

# Питон
__pycache__/
*.pyc
.venv/
venv/

# ОС / редакторы
.DS_Store
Thumbs.db
.vscode/
.idea/

# Логи / временное
*.log
tmp/

# Рантайм-кэш скачанных лого/фото контрагентов (эфемерно на Render, не исходники)
static/images/

# Локальные лаунчеры / служебное (не нужны на Render)
*.bat
tunnel_keeper.py
contact_preview.html
_probe*.py
_fill_*.py
